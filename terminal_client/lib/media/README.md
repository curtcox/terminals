# media

Audio playback and WebRTC engine management.

`playback.dart` decodes and plays server-sent audio (TTS, alerts) via the audioplayers package. `webrtc_engine.dart` manages the Pion/flutter_webrtc peer connection lifecycle for real-time audio/video streams.
