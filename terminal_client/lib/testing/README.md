# testing

Test harness helpers shared between client-side tests.

`frame_counter_harness.dart` decodes the 8-byte frame-counter prefix injected by the server harness, used in transport integration tests to assert frame delivery counts.
