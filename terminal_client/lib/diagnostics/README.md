# diagnostics

Client-side bug reporting and build metadata.

`bug_report_chrome.dart` captures and formats bug reports (clipboard, screenshots). `build_metadata.dart` exposes compile-time build SHA and date. `client_chrome.dart` wraps Chrome/web-specific diagnostics hooks. `diagnostic_clipboard.dart` copies structured diagnostic data to the clipboard.
