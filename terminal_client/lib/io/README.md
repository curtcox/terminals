# io

Sensor telemetry streaming to the control stream.

`sensor_streamer.dart` builds typed sensor telemetry envelopes (accelerometer, GPS, etc.) from platform sensor events and writes them to the outgoing control stream.
