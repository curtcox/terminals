# Terminals — Use Cases

Use cases derived from the [Master Plan](masterplan.md), now split one file
per family under [`usecases/`](usecases/). Stable IDs (`C1`, `M5`, `PL27`, …)
are referenced from plan frontmatter as `validation: automated:<ID>`.

The canonical, auto-generated index lives at
[`usecases/INDEX.md`](usecases/INDEX.md) (regenerated via
`make usecases-index`).

This file is kept as a thin pointer for backwards-compatible links — the
content was moved on 2026-05-01 (audit finding F2).
