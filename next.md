Flesh out the pluggable AI backend interfaces (Phase 5 — Voice Assistant). The current `internal/ai` package only exposes a single `NoopBackend.Query` shim. The masterplan calls for distinct interfaces — `SpeechToText`, `TextToSpeech`, `LLM`, `VisionAnalyzer`, `SoundClassifier` — each with a noop implementation and unit tests, and an `AIBackend` aggregate (or separate fields) wired into `scenario.Environment` so scenarios can depend on the specific capability they need rather than a single generic `Query` method. Then update `VoiceAssistantScenario` to consume the `LLM` interface (keeping the existing `AIBackend.Query` shim in place for backward compatibility during the transition) and add a runtime test that exercises the full STT → LLM → TTS pipeline against the noop implementations through the control stream.
