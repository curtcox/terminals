# Use Case Validation Matrix

_Auto-generated by `scripts/generate-validation-matrix.py` from `usecases/*.md`, plan frontmatter (`validation: automated:<ID>`), and the `all_ids` list in `scripts/usecase-validate.yaml`. Do not edit by hand — update the source files and run `make validation-matrix`._

This matrix maps `usecases/*.md` IDs to current automated validation coverage.

Primary command:

```bash
make usecase-validate USECASE=<ID>
# or
make usecase-validate USECASE=all
```

## Automated IDs

Coverage depth labels:

- `Smoke`: proves a narrow server loop or command path.
- `Transport`: proves generated/wire control-plane behavior.
- `Scenario`: proves scenario matching and server-side side effects.
- `Contract`: proves app/package/runtime contract surfaces.
- `Simulation`: proves lifecycle behavior against synthetic time/events.
- `Full`: covers trigger, placement, UI, scheduling, side effects, and expiry/cancel/resume behavior.

| ID | Scenario | Validation Command | Primary Evidence | Coverage Depth |
|---|---|---|---|---|
| AB1 | have kitchen and front-of-house devices intercomed together | `make usecase-validate USECASE=AB1` | kitchen/front-of-house intercom: audio route opened from front_of_house to kitchen_terminal; stop closes route; harness-backed evidence | Simulation |
| AB2 | broadcast announcements to the sales floor via PA mode | `make usecase-validate USECASE=AB2` | PA broadcast to sales floor: pa_system scenario started on manager_terminal; broadcast delivers 'PA from manager_terminal' to all sales floor devices | Simulation |
| AB3 | display guest welcome messages or room information on lobby devices | `make usecase-validate USECASE=AB3` | guest welcome display: photo_frame scenario started on lobby_display device; broadcast confirms display mode active | Simulation |
| AB4 | monitor camera feeds from loading docks and aisles in a multi-window grid | `make usecase-validate USECASE=AB4` | multi-window camera grid: multi_window scenario started on supervisor_terminal; video routes established from all four dock cameras | Simulation |
| AB5 | set up audio monitoring to detect alarm or glass-break sounds after hours | `make usecase-validate USECASE=AB5` | after-hours alarm monitoring: audio_monitor armed for glass_break; FakeSoundClassifier emits glass_break; alert broadcast to security_terminal | Simulation |
| AB6 | say "timer 12 minutes table 5" and be reminded when food should be checked | `make usecase-validate USECASE=AB6` | voice timer for food service: voice 'set a timer for 12 minutes table 5' sets timer with label; fake-clock advance fires 'Timer done!' broadcast | Simulation |
| AB7 | use voice to announce "patient Smith, room 3 is ready" to the waiting area device | `make usecase-validate USECASE=AB7` | voice announcement to waiting area: announcement scenario started; waiting_area_terminal receives announcement_audio RouteStream; no duplicate routes | Simulation |
| B1 | file a bug report from the device I am currently using, via whatever input it has (button, gesture, shake, keyboard shortcut, or voice) | `make usecase-validate USECASE=B1` | transport input bug-report action coverage for modality parity | Scenario |
| B2 | file a bug report about a *different* device that has no working input by scanning its QR code, tapping an NFC tag, using the admin dashboard on another device, or speaking the target device's name into voice | `make usecase-validate USECASE=B2` | diagnostics bug-report service cross-device subject/offline coverage | Scenario |
| B3 | autodetect probable device failures (heartbeat timeout, never-registered, reconnect loop, repeated control errors) and open a pending bug report with the subject's last-known state | `make usecase-validate USECASE=B3` | diagnostics service autodetect merge test | Scenario |
| B4 | view every filed bug report together with the subject device's last-known capabilities, UI, state, and event-log trace via `/admin/bugs` | `make usecase-validate USECASE=B4` | admin bug intake/list/detail and filter tests | Scenario |
| B5 | dial a reserved extension on the SIP bug line and describe the problem verbally | `make usecase-validate USECASE=B5` | admin bug intake JSON SIP source + transcript hints test | Scenario |
| C1 | press a button or say "intercom to kitchen" and have two-way audio with the kitchen device | `make usecase-validate USECASE=C1` | internal/transport generated+wire integration tests; internal/usecasevalidation harness-backed evidence test | Transport |
| C2 | say "announce: dinner is ready" and have it broadcast to every device | `make usecase-validate USECASE=C2` | whole-house announcement: 1 speaker + 3 receivers; harness-backed broadcast routing and no-duplicate-delivery evidence | Simulation |
| C3 | activate PA mode so my microphone streams to all speakers simultaneously | `make usecase-validate USECASE=C3` | PA relay, voice start, voice stop alias tests | Transport |
| C5 | start a video call between two devices in the house | `make usecase-validate USECASE=C5` | TestGeneratedSessionInternalVideoCallStartSetUIAndHangupFlow | Transport |
| D1 | set an idle tablet to photo frame mode that rotates pictures | `make usecase-validate USECASE=D1` | photo-frame config + heartbeat rotation tests | Scenario |
| D2 | have the photo frame automatically yield to higher-priority scenarios and resume afterward | `make usecase-validate USECASE=D2` | photo frame yields to higher-priority scenario (alert/call) and resumes afterward | Scenario |
| D3 | see a clock or standby screen on idle devices | `make usecase-validate USECASE=D3` | standby/clock mode activated by voice or manual command; server notifies requesting device | Transport |
| I3 | report my full capability manifest (screen, mic, camera, sensors, etc.) on connection | `make usecase-validate USECASE=I3` | capability manifest on connection: server records full hardware capability set declared by connecting device | Transport |
| I4 | query the device registry for devices matching a required capability set | `make usecase-validate USECASE=I4` | device registry capability query: placement engine returns only devices matching required capability set; generated+wire variants register two devices and verify filtered routing | Transport |
| I6 | preempt lower-priority scenarios and suspend them for later resumption | `make usecase-validate USECASE=I6` | preemption: engine suspends lower-priority scenario routes when higher-priority scenario starts and resumes them on stop | Scenario |
| M1 | say "tell me when the dishwasher stops" and have the system listen for silence | `make usecase-validate USECASE=M1` | silence classifier integration test | Scenario |
| M2 | say "tell me when the dryer beeps" and have the system listen for a specific sound | `make usecase-validate USECASE=M2` | audio monitor runtime test for dryer beep detection | Scenario |
| M3 | say "red alert" and have every screen turn red with an alarm sound | `make usecase-validate USECASE=M3` | generated+wire red alert integration tests | Transport |
| M4 | say "stand down" or tap any device to dismiss a red alert | `make usecase-validate USECASE=M4` | generated+wire voice stop/stand-down tests | Transport |
| M5 | have the system watch a room's camera for activity during certain hours | `make usecase-validate USECASE=M5` | camera activity watch: time-window policy gates alerts; harness-backed sensor routing and suppression evidence | Simulation |
| PL1 | open a live text chat room for a topic like "kitchen", "kids", or "movie night" from any device | `make usecase-validate USECASE=PL1` | capability message room/thread/unread acknowledgement lifecycle test | Contract |
| PL8 | invite another device into a shared live session where both participants can see the same server-driven view | `make usecase-validate USECASE=PL8` | interactive session join/leave and control lifecycle capability tests | Contract |
| PL20 | save reusable icons, signs, and visual templates for announcements and routines | `make usecase-validate USECASE=PL20` | capability artifact template save/apply and artifact history tests | Contract |
| P1 | open an interactive REPL session on a laptop or Chromebook that connects to the server | `make usecase-validate USECASE=P1` | generated+wire terminal transition tests | Transport |
| P2 | have multiple REPL sessions on one device or access one session from multiple devices | `make usecase-validate USECASE=P2` | REPL session mobility and coexistence lifecycle test | Scenario |
| P3 | ask the LLM questions, generate code, and have it propose Terminals commands on my behalf, with read-only calls auto-executed and every mutating command gated on my explicit approval | `make usecase-validate USECASE=P3` | REPL ai ask/gen commands plus mutating approval-gate metadata test | Scenario |
| P4 | pick my LLM provider (OpenRouter or Ollama) and model with a sticky selection that survives detach/reattach | `make usecase-validate USECASE=P4` | sticky REPL AI provider/model survives detach and reattach | Scenario |
| S1 | say "show all cameras" and see a grid of every device's camera on one screen | `make usecase-validate USECASE=S1` | generated+wire voice show-all-cameras tests | Transport |
| S2 | tap a camera cell in the grid to isolate its audio | `make usecase-validate USECASE=S2` | generated+wire focus-action routing tests | Transport |
| S3 | have camera feeds mixed into a single audio track by default | `make usecase-validate USECASE=S3` | generated+wire multi-window audio mix tests | Transport |
| AA1 | trigger scenarios via the server API based on external events (calendar, webhook, sensor) | `make usecase-validate USECASE=AA1` | external automation agent triggers announcement scenario via manual API; display terminal receives announcement_audio route; YAML scenario in internal/usecasevalidation/testdata/aa1-webhook-announce.yaml | Simulation |
| AA2 | subscribe to sound classification events and route notifications to other systems (Slack, email) | `make usecase-validate USECASE=AA2` | monitoring agent arms audio monitoring via manual API; fake classifier emits dryer_beep; broadcast targeted at agent device ID | Simulation |
| AA3 | use the LLM backend to interpret ambiguous voice commands and map them to scenarios | `make usecase-validate USECASE=AA3` | AI agent resolves ambiguous voice command via fake LLM; LLM-decoded announce intent routes announcement_audio to a second terminal | Simulation |
| AA4 | create, modify, and cancel timers and reminders via the server API | `make usecase-validate USECASE=AA4` | scheduling agent creates and cancels a timer via manual API; cancelled timer produces no 'Timer done!' broadcast; YAML scenario in internal/usecasevalidation/testdata/aa4-timer-cancel.yaml | Simulation |
| AA5 | process camera frames and generate alerts or annotations on the viewing device | `make usecase-validate USECASE=AA5` | vision analysis agent: FakeVisionAnalyzer returns caption and labels; camera_activity sensor triggers analysis; broadcast verified with caption, labels, and agent device target | Simulation |
| AA6 | run integration tests that simulate multiple devices connecting and exchanging IO | `make usecase-validate USECASE=AA6` | admin scripts run over seeded sim/store/ui/bus fixture plus mutating Layer 2 message, board, artifact, canvas, and session paths | Simulation |
| UI1 | see an idle tablet show server-driven ambient UI with a reachable corner menu affordance | `make usecase-validate USECASE=UI1` | idle photo-frame SetUI includes scoped corner affordance (terminal-ui plan) | Transport |
| UI2 | tap the corner affordance and open the menu overlay | `make usecase-validate USECASE=UI2` | corner.open toggles menu overlay claim without disturbing main | Transport |
| UI3 | have default overlay routing keep background audio live while pointer goes to the menu | `make usecase-validate USECASE=UI3` | menu overlay default MIXED policy: main pointer blocked, audio live | Transport |
| UI4 | toggle privacy mode and have mic/camera capability withdrawn atomically on the wire | `make usecase-validate USECASE=UI4` | privacy toggle: proto withdrawal, server routes, post-cutover frame drop | Transport+Client |
| UI5 | stay in privacy mode without wake-word streaming or client chrome privacy indicators | `make usecase-validate USECASE=UI5` | privacy mode: no VoiceAudio after withdrawal; no client-chrome privacy indicators | Client |
| UI6 | speak a wake phrase on one terminal and have the server react | `make usecase-validate USECASE=UI6` | wake-word path activates scenario (single client) | Transport |
| UI7 | have duplicate wake detections from two nearby terminals dedupe to one intent | `make usecase-validate USECASE=UI7` | wake-word dedupe across two clients dispatches at most one intent | Transport |
| UI8 | rotate or resize the display and emit capability deltas the server can use | `make usecase-validate USECASE=UI8` | capability delta on rotation/resize; overlay survives orientation delta | Transport+Client |
| UI9 | reconnect while the menu overlay is open and see both layers restored | `make usecase-validate USECASE=UI9` | reconnect restores main + overlay UI state; harness-backed evidence (RECON-1) | Simulation |
| UI10 | rely on CI to enforce the corner affordance reachability invariant for every registered main-layer scenario | `make usecase-validate USECASE=UI10` | registry corner affordance reachability invariant for every main-layer scenario | Scenario |
| T1 | say "set a timer for 10 minutes" | `make usecase-validate USECASE=T1` | due-timer loop; transport run_due_timers; kitchen timer package smoke test; voice-path fake-clock harness (timer fires via synthetic time advance) | Simulation |
| T2 | say "remind me to check the oven at 3 PM" | `make usecase-validate USECASE=T2` | timer reminder: fake-clock advance triggers ProcessDueTimers; broadcast confirms 'Timer done!' without real elapsed time; YAML scenario in internal/usecasevalidation/testdata/t2-timer-reminder.yaml | Simulation |
| T3 | configure the system to monitor my child's morning routine via camera at 7 AM on school days | `make usecase-validate USECASE=T3` | school-morning monitor: no camera activity by alert time notifies parent; activity before alert cancels notification; YAML scenarios in internal/usecasevalidation/testdata/t3-t4-school-morning.yaml and t3-activity-cancels-alert.yaml | Simulation |
| T4 | have the system warn my child via speaker ("the bus comes in 10 minutes") | `make usecase-validate USECASE=T4` | school-morning warning: bus-warning broadcast fires to child-room at configured warning time via synthetic clock advance; YAML scenario in internal/usecasevalidation/testdata/t3-t4-school-morning.yaml | Simulation |
| V1 | say a wake word and ask a question on any device | `make usecase-validate USECASE=V1` | voice assistant wake-and-answer: 'assistant <question>' triggers VoiceAssistantScenario; FakeLLM queried; response broadcast back to source device | Simulation |
| V2 | ask for a recipe by voice and see it displayed on the nearest screen | `make usecase-validate USECASE=V2` | voice recipe request: 'assistant how do I make <dish>' triggers VoiceAssistantScenario; FakeLLM returns recipe; broadcast back to kitchen terminal | Simulation |
| V3 | ask the system about the weather, news, or general knowledge | `make usecase-validate USECASE=V3` | voice general-knowledge query: 'assistant <question>' triggers VoiceAssistantScenario; FakeLLM returns answer; broadcast back to requesting device | Simulation |

## Planned / Not Yet Automated

The following planned IDs are not declared automated by any plan and are not wired into `scripts/usecase-validate.sh`:

`AH1`, `AH2`, `AH3`, `AH4`, `AH5`, `AH6`, `AH7`, `AH8`, `AH9`, `AH10`, `AH11`, `AH12`, `AH13`, `AH14`, `AH15`, `AH16`, `AH17`, `AO1`, `AO2`, `AO3`, `AO4`, `AO5`, `AO6`, `AO7`, `C4`, `C6`, `I1`, `I2`, `I5`, `I7`, `I8`, `I9`, `I10`, `I11`, `PL2`, `PL3`, `PL4`, `PL5`, `PL6`, `PL7`, `PL9`, `PL10`, `PL11`, `PL12`, `PL13`, `PL14`, `PL15`, `PL16`, `PL17`, `PL18`, `PL19`, `PL21`, `PL22`, `PL23`, `PL24`, `PL25`, `PL26`, `PL27`.

Use `make all-check` as the baseline repository gate while dedicated use-case mappings are added.
