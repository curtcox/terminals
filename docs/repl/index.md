# REPL

The Terminals REPL is a server-side control-plane shell.

See `quickstart.md` for a basic flow, `sessions.md` for lifecycle semantics, and `commands/` for command groups.

See `deferred-work.md` for the durable ledger of deferred REPL capability concerns and re-open triggers.
