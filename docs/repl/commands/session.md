# session

Typed generalized interactive session commands:

- `session ls`
- `session create <kind> <target>`
- `session join <session> <participant>`
- `session leave <session> <participant>`

Use `help session` for flags and JSON output options.
