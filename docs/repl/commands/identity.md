# identity

Use `identity` to inspect known identities and resolve audience selectors.

- `identity ls`
- `identity resolve <audience>` where `<audience>` can be `all`, `id:<id>`, `group:<group>`, or `alias:<alias>`.
