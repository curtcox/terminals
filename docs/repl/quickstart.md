# Quickstart

1. Start a terminal scenario from a keyboard/display device.
2. Run `help` to inspect available commands.
3. Use `devices ls`, `activations ls`, and `claims tree` to inspect runtime state.
4. Use `sessions ls` to inspect active REPL sessions.
