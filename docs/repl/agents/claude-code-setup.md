# Claude Code Setup

1. Add a new MCP server entry in Claude Code.
2. Configure either stdio (`mcp-stdio`) or Streamable HTTP (`/mcp`).
3. Connect and run `repl_describe` / `repl_complete` to verify discovery.
4. Confirm mutating commands prompt for approval.
