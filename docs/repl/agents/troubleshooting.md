# Troubleshooting

## Mutating command rejected with `unsupported_client`

Your client did not negotiate a supported approval path. Enable elicitation support or confirmation-id fallback support.

## Tool missing after server update

Reconnect your MCP client so it refreshes the tool catalog.

## Docs output is hard to parse

MCP dispatch uses Markdown docs rendering mode for `docs open` and `docs search`.
